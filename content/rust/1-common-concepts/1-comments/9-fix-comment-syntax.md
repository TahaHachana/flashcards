# Fix Comment Syntax

Fix the comment syntax in this code:
```rust
/* This is wrong */
let x = 5;
```

---

```rust
// This is correct
let x = 5;
```
While `/* */` works, `//` is the idiomatic way in Rust.
