# Doc Comments and Tooling

How do documentation comments integrate with Rust tooling?

---

Documentation comments (`///`) are processed by `rustdoc` to automatically generate HTML documentation for a crate's public API.