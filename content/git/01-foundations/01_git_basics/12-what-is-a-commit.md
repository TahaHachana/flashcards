## What Is a Commit?

What is a Git commit?

---

A **commit** is a snapshot of the entire project at a specific point in time, stored permanently in the Git repository. Each commit is identified by a unique SHA-1 hash and records:
- The snapshot of staged files
- The author and timestamp
- A commit message describing the change
- A pointer to the parent commit(s)

