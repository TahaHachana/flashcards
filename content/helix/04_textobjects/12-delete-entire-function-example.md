## Delete Entire Function Example

How would you delete an entire function including its signature in Helix?

---

`maf` followed by `d` - Select around (`ma`) the function textobject (`f`), then delete (`d`).

