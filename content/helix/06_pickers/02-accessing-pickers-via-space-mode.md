## Accessing Pickers via Space Mode

How are most pickers accessed in Helix?

---

Most pickers are accessed via keybindings in space mode (press Space in normal mode). For example: `Space-f` for file picker, `Space-/` for global search picker, `Space-b` for buffer picker.

