## Delete A Big Block With Marks

How can marks help delete a large block without counting lines?

---

Jump to one end and set a mark (e.g. `ma`), move to the other end, then `d`a` to delete the whole range — no need to compute a line count like `47dd`.

