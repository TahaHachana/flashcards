## The Y Inconsistency

Why is `Y` the odd one out compared with `D` and `C`?

---

`Y` does NOT yank to end of line — it yanks the whole line (same as `yy`). To yank from the cursor to end of line, use `y$`.

