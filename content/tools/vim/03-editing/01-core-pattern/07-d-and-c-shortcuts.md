## D And C Shortcuts

What do `D` and `C` do, and what longer commands are they equivalent to?

---

`D` deletes from the cursor to end of line (= `d$`); `C` changes from the cursor to end of line (= `c$`).

