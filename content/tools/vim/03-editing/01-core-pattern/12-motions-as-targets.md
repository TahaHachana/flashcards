## Motions As Targets

Give the effect of `d}`, `cG`, and `d^`.

---

`d}` deletes up to the next paragraph; `cG` changes to the end of the buffer; `d^` deletes back to the first non-blank character of the line.

