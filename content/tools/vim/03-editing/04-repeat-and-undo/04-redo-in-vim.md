## Redo In Vim

How do you redo (reverse an undo) in Vim, and how deep does Vim's undo go?

---

`CTRL-R` redoes. Vim supports "infinite" / multi-level undo, so you can step back through many changes and redo forward.

