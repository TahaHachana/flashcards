## Numbered Deletion Registers

What do numbered registers `1`–`9` hold, and which is which?

---

The last nine deletions. Register `1` is the most recent deletion, `2` the second-most-recent, and so on through `9`.

