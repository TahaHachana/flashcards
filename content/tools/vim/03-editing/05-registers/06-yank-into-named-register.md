## Yank Into A Named Register

How do you yank the current line into register `d`, and later put it before the cursor?

---

`"dyy` to yank into register d; `"dP` to put its contents before the cursor.

