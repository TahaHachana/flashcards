## The Three Visual Modes

What do `v`, `V`, and `CTRL-V` each select?

---

`v` = charwise (one character at a time), `V` = linewise (whole lines), `CTRL-V` = blockwise (a rectangular column block).

