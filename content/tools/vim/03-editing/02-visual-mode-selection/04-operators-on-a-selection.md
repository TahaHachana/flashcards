## Operators On A Selection

After selecting text in visual mode, what do `d`, `y`, `c`, `>`, and `<` do?

---

`d` deletes the selection, `y` yanks (copies) it, `c` changes it, `>` indents it one shiftwidth right, `<` indents it one shiftwidth left.

