## Extending A Selection

Once in visual mode, how do you grow the highlighted region?

---

Use any motion command. Each motion (e.g. `w`, `}`, `/pattern`, `j`) moves the cursor and extends the highlight to the new position.

