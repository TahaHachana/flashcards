## Text-Object Selectors

Which visual-mode selectors grab a sentence or a paragraph around the cursor?

---

`as`/`is` for a sentence / inner sentence, and `ap`/`ip` for a paragraph / inner paragraph.

