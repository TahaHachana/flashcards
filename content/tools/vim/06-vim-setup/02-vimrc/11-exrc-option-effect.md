## exrc Option Effect

What does setting the `exrc` option enable?

---

It makes Vim also read configuration files (`.vimrc`, `.gvimrc`, `.exrc`) from the current directory, allowing per-project config.

