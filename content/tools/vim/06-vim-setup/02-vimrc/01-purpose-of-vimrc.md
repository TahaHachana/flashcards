## Purpose Of vimrc

What is the `.vimrc` file for?

---

It permanently configures Vim's editing characteristics — options, global variables, functions, abbreviations, and key mappings — so they apply on every launch.

