## Search Behavior Defaults

Which three default options control case-sensitivity, wrapping, and wildcards in searches?

---

`ignorecase`, `wrapscan`, and `magic`; reverse them with `noignorecase`, `nowrapscan`, and `nomagic`.

