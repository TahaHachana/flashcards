## smartcase Behavior

What does `smartcase` do, and which option does it depend on?

---

It overrides `ignorecase` when the search pattern contains an uppercase character; it is only meaningful when `ignorecase` is on.

