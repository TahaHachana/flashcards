## Vertical Bar Restriction

Why can't you use a vertical bar `|` in an insert-mode map?

---

`|` always has special meaning as a separator of multiple ex commands and cannot be escaped in this context.

