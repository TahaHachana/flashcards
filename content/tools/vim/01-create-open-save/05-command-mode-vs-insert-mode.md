## Command Mode vs Insert Mode

What are the two primary modes in Vim, and how does Vim start?

---

1. Command mode — every keystroke is a command (the default/starting mode).
2. Insert mode — keystrokes are entered as text into the buffer.

Vim always starts in command mode. You enter insert mode with commands like `i`, and return to command mode by pressing ESC.

