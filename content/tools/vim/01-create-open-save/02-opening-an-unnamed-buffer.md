## Opening an Unnamed Buffer

What happens when you run `vim` with no filename argument?

---

Vim opens an empty, unnamed buffer. You can assign a name to it later when you write (save) the buffer to disk. Until then, the file has no name on the filesystem.

