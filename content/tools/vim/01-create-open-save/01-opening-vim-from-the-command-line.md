## Opening Vim from the Command Line

What is the basic syntax for opening a file with Vim from the shell?

---

```
$ vim [filename]
$ vi [filename]
```

The brackets mean the filename is optional. On modern systems, `vi` is usually just a link to Vim. Press ENTER after the command to tell the shell you are done.

