## Repeat With Slash Or Question

Besides `n`/`N`, how can you repeat the last search forward or backward?

---

Press `/` then `ENTER` to repeat forward, or `?` then `ENTER` to repeat backward (reusing the last pattern).

