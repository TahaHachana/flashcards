## Substring Matching

If you search for `red`, will it match only the whole word "red"?

---

No — a pattern is just a character string, so `red` matches `red` and also the `red` inside `occurred`.

