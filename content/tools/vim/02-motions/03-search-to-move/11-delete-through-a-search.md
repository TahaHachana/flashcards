## Delete Through A Search

What does `d/while` followed by `ENTER` do?

---

It deletes from the cursor up to and through the next occurrence of `while`. The deletion is character-based, not whole lines.

