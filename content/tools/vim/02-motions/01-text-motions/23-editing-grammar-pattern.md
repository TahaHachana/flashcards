## Editing Grammar Pattern

What is the general pattern for composing an edit from an operator and a motion?

---

`[count] operator [count] motion` — e.g., `2yw`, `d}`, `c$`. The motion tells the operator how much text to act on.

