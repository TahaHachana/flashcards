## cw Equals ce

Although `w` and `e` are different motions, why do `cw` and `ce` behave identically?

---

Both change text from the cursor to the end of the current word — for the change operator, `cw` is treated the same as `ce`.

