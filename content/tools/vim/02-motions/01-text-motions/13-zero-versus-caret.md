## Zero Versus Caret

What is the difference between `0` and `^`?

---

`0` moves to the very first position of the line (even if it is blank). `^` moves to the first nonblank character, skipping leading spaces and tabs.

