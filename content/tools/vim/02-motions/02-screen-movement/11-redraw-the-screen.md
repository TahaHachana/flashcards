## Redraw The Screen

Which command redraws the screen without scrolling, and what is the Vim caveat?

---

`CTRL-L` redraws without scrolling. Caveat: `CTRL-R` redraws in classic vi but means **redo** in Vim, so use `CTRL-L` in Vim.

