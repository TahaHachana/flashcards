## Substitute Commands

What do `s`, `S`, and `R` do?

---

`s` deletes the character under the cursor and substitutes text (same as `c SPACE`); `S` deletes the whole line and substitutes (same as `cc`); `R` overstrikes existing characters until `ESC`.

