## The Third Mode

What is often called Vim's "third mode"?

---

The colon / ex command line — the bottom-line prompt where you type ex commands (and searches). Its commands are echoed and need ENTER to run.

