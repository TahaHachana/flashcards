## Mode On File Open

When you first open a file in Vim, which mode are you in?

---

Command (normal) mode — the editor is waiting for a command, not for text. This is why typing immediately does not just enter text.

