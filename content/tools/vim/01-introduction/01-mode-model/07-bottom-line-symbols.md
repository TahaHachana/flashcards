## Bottom-Line Command Symbols

Which symbols start a bottom-line (echoed) command, and what does each begin?

---

```
:   an ex command          (e.g. :wq)
/   a forward search       (e.g. /pattern)
?   a backward search      (e.g. ?pattern)
```

