## What Is The Buffer

What is the "buffer," and how does it relate to your file on disk?

---

The buffer is an in-memory working copy of the file. All edits happen there; the file on disk is unchanged until you write (save) the buffer.

