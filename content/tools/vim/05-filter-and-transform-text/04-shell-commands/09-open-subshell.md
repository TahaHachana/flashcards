## Open A Full Subshell

What command opens a full interactive subshell from Vim, and how do you return to the editor?

---

`:sh` opens a subshell. Return to Vim with `CTRL-D` (or type `exit`). It works even from gvim.

