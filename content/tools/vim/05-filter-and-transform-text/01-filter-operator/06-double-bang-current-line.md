## The !! Current-Line Object

Cloze: To filter only the current line through a command, the special object syntax is {{!!command}}.

---

`!!command` — the second `!` is a special text object meaning "the current line."

