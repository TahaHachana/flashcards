## Filtering From Visual Mode

What are the steps to filter a selection through `sort` using Visual mode?

---

Select lines with `V`, type `!`, type `sort`, then press ENTER. The selection is replaced by the sorted output.

