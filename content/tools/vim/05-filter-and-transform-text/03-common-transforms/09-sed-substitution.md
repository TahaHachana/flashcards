## sed Substitution Flags

What is the difference between `sed 's/old/new/'` and `sed 's/old/new/g'`?

---

The first replaces only the FIRST match on each line; the `g` flag in the second replaces ALL matches on each line.

