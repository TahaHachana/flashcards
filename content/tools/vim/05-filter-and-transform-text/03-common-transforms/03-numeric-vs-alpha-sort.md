## Numeric Versus Alphabetic Sort

What is the difference between `sort` and `sort -n`?

---

`sort` orders lines lexicographically (so "100" comes before "9"); `sort -n` orders them numerically (so 9 comes before 100).

