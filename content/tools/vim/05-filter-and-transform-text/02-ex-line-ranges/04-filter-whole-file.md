## Filter The Whole File

Cloze: To filter the entire file through `sort`, use {{:%!sort}}, where `%` means {{the whole file (line 1 through last line)}}.

---

`:%!sort`; `%` is shorthand for `1,$`, the whole file.

