## Next And Previous Buffer

Which commands move to the next and previous buffers in the list?

---

`:bnext` (`:bn`) moves to the next buffer; `:bprevious` (`:bp`, also `:bNext`) moves to the previous buffer.

