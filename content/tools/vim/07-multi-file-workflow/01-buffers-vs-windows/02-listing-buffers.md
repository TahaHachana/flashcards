## Listing Buffers

Which three interchangeable commands list all current buffers?

---

`:ls`, `:files`, and `:buffers`. Append `!` (e.g. `:ls!`) to also show unlisted buffers such as the help buffer.

