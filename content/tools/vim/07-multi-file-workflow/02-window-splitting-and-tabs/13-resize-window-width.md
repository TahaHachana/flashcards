## Resize Window Width

Which keystrokes decrease and increase window width, and what's the mnemonic?

---

`CTRL-W <` decreases width and `CTRL-W >` increases width. Mnemonic: `<<` shift left (narrower), `>>` shift right (wider). `CTRL-W |` maximizes width.

