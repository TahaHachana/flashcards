## No Ctrl-W Ctrl-C

Why is there no `CTRL-W CTRL-C` close-window keystroke?

---

Because Vim uses `CTRL-C` to cancel commands, so `CTRL-W CTRL-C` just cancels the command. Use `CTRL-W c` (with a lowercase c, no Ctrl) instead.

