## Open Files In Separate Tabs At Launch

Which command-line option opens each file in its own tab at startup?

---

The `-p` option, e.g. `vim -p ~/.vimrc /etc/hosts` opens each file in a separate tab.

