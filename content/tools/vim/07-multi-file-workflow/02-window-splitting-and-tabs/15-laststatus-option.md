## Laststatus Option

What do the values of the `laststatus` option mean?

---

`0` = never show a status line; `1` = show one only when there are multiple windows (default); `2` = always show a status line for every window. Best set in `.vimrc`.

