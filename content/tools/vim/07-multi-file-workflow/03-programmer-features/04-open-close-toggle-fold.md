## Open Close Toggle Fold

Which commands open, close, and toggle a single fold?

---

`zo` opens one fold, `zc` closes one fold, and `za` toggles one fold. The uppercase versions (`zO`, `zC`, `zA`) act recursively on nested folds.

