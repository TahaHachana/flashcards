## Fold A Brace Block

With the cursor on a brace, how do you fold the whole `{ … }` block?

---

`zf%` — `zf` starts a fold and `%` jumps to the matching brace, so the fold covers the entire block.

