## Shift Lines

How do `>>` and `<<` work, and how do you shift five lines right?

---

`>>` shifts a line right and `<<` shifts it left by `shiftwidth` (default 8). Prefix with a count: `5>>` shifts five lines right.

