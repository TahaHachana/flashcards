## Simplest Color Scheme Script

What is the simplest possible color-scheme script you can put in a config file?

---

A single command, e.g. `:colorscheme desert`, which applies that scheme on every launch.

