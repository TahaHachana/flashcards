## Defining Functions

How do you define and call a Vim function, and what is the naming rule?

---

Define with `function Name(args) … endfunction` and call with `call Name()`. User-defined function names must begin with a capital letter.

