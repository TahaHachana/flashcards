## Strftime Hour

Which built-in function returns the current hour, and how is it called?

---

`strftime()` with the format `%H`, written `strftime("%H")`. Its optional second argument is epoch seconds and defaults to the current time.

