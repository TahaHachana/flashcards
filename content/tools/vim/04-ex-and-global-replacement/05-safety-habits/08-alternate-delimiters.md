## Alternate Delimiters Allowed

Which characters may you use as a substitute delimiter instead of `/`?

---

Any nonalphanumeric, nonspace character except backslash (`\`), double quote (`"`), and vertical bar (`|`).

