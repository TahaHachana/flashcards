## The Backslash Escape

What does `\` do, and how do you match a literal period or a literal backslash?

---

`\` escapes the following special character so it is treated literally. `\.` matches a literal period; `\*` a literal asterisk; `\\` a literal backslash.

