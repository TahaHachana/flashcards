## The Caret And Dollar Anchors

What do `^` and `$` anchor to in a regular expression?

---

`^` at the start of a regex requires the match at the beginning of the line; `$` at the end requires the match at the end of the line. They are called anchors. Elsewhere in the expression each stands for itself.

