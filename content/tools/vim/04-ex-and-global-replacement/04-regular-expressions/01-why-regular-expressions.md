## Why Use Regular Expressions

Why use a regular expression instead of a literal string search?

---

A literal search can match unintended text (e.g. "top" inside "stopper"). Regular expressions match variable patterns, letting you search for words in context. They work with `/`, `?`, `:g`, and `:s`, and with Unix tools like grep, sed, and awk.

