## Escaping Inside Brackets

Inside `[ ]`, which three characters still need special handling, and how?

---

`\`, `-`, and `]`. Escape `\` with a backslash; place `-` first (or escape it) to use it literally; place `]` first to use it literally. Most other metacharacters lose their special meaning inside brackets.

