## Vim As ex

How does Vim relate to ex?

---

Vim provides the underlying ex editor with many enhancements over the original. On systems where `vi` is Vim, `ex` usually also invokes Vim in ex mode.

