## Other Commands g Can Drive

Besides `:s`, which ex commands can `:g` be combined with?

---

`:d` (delete), `:m` (move), `:co` (copy), and others — letting you make global deletions, moves, and copies, not just substitutions.

