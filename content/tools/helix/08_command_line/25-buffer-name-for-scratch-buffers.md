## Buffer Name for Scratch Buffers

What value does the `%{buffer_name}` variable expand to for scratch buffers?

---

For scratch buffers (unsaved/unnamed buffers), `%{buffer_name}` expands to `[scratch]` instead of a file path.

