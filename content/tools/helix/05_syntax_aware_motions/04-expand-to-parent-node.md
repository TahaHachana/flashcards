## Expand to Parent Node

What does `Alt-o` do and give an example?

---

Expands the selection to the parent node in the syntax tree. For example, if `arg1` is selected in `func(arg1, arg2, arg3)`, `Alt-o` expands to select `(arg1, arg2, arg3)` (the arguments node).

